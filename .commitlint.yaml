formatter: default

rules:
  - type-enum

severity:
  default: error

settings:
  type-enum:
    argument:
      - feat
      - fix
      - docs
      - style
      - refactor
      - perf
      - test
      - build
      - ci
      - chore
      - revert
